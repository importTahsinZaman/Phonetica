import { View } from "react-native";
import CustomText from "./CustomText";

const TranslateContainer = () => {
  return (
    <View>
      <CustomText>Translate Container</CustomText>
    </View>
  );
};

export default TranslateContainer;
